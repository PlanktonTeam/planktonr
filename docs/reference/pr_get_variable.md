# Generic function to get the Variable attribute

Generic function to get the Variable attribute

## Usage

``` r
pr_get_variable(x)

# S3 method for class 'planktonr_dat'
pr_get_variable(x)
```

## Arguments

- x:

  A `planktonr_dat` object.

## Value

The `Variable` attribute from the object.
