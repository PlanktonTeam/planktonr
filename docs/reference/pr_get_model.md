# Extract fitted model objects from planktonr data with trend analysis

Extract fitted model objects from planktonr data with trend analysis

## Usage

``` r
pr_get_model(x)

# S3 method for class 'planktonr_dat'
pr_get_model(x)
```

## Arguments

- x:

  A `planktonr_dat` object.

## Value

The `Model` attribute from the object.
