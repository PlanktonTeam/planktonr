# Generic function to get the Survey attribute

Generic function to get the Survey attribute

## Usage

``` r
pr_get_survey(x)

# S3 method for class 'planktonr_dat'
pr_get_survey(x)
```

## Arguments

- x:

  A `planktonr_dat` object.

## Value

The `Survey` attribute from the object.
