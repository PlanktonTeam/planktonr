% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils_data.R
\name{pr_get_data}
\alias{pr_get_data}
\title{Get plankton and biogeochemical data from IMOS surveys}
\usage{
pr_get_data(
  Survey = "NRS",
  Type = "Phytoplankton",
  Variable = NULL,
  Subset = NULL,
  Format = "all"
)
}
\arguments{
\item{Survey}{Survey program to retrieve data from:
\itemize{
\item \code{"NRS"} - National Reference Stations (default). Coastal monitoring stations
around Australia with monthly sampling.
\item \code{"CPR"} - Continuous Plankton Recorder. Ship-towed sampler providing
broad-scale spatial coverage along shipping routes.
\item \code{"SOTS"} - Southern Ocean Time Series. Deep-water mooring site south of
Tasmania for monitoring Southern Ocean ecosystems.
\item \code{"Coastal"} - Coastal Seas microbial monitoring sites.
\item \code{"GO-SHIP"} - Global Ocean Ship-based Hydrographic Investigations Program.
}}

\item{Type}{Data type to retrieve (case-insensitive for Phytoplankton/Zooplankton):
\itemize{
\item \code{"Phytoplankton"} - Phytoplankton abundance or biovolume (NRS, CPR, SOTS)
\item \code{"Zooplankton"} - Zooplankton abundance (NRS, CPR, SOTS)
\item \code{"Chemistry"} - Water chemistry including nutrients (NRS, Coastal)
\item \code{"Pigments"} - HPLC photosynthetic pigments (NRS only)
\item \code{"Pico"} - Picophytoplankton from flow cytometry (NRS only)
\item \code{"Micro"} - Microbial community data (NRS, Coastal, GO-SHIP)
\item \code{"TSS"} - Total Suspended Solids (NRS only)
\item \code{"CTD"} - CTD profiles: temperature, salinity, fluorescence (NRS only)
}}

\item{Variable}{Variable to retrieve (required for Phytoplankton/Zooplankton):
\itemize{
\item \code{"abundance"} - Cell or individual counts
\item \code{"biovolume"} - Cell biovolume in cubic micrometres (Phytoplankton only)
}}

\item{Subset}{Data aggregation level (required for Phytoplankton/Zooplankton):
\itemize{
\item \code{"raw"} - Raw taxonomic data as identified by analysts
\item \code{"htg"} - Higher taxonomic groups (e.g., diatoms, dinoflagellates)
\item \code{"genus"} - Aggregated to genus level
\item \code{"species"} - Aggregated to species level
\item \code{"copepods"} - Copepod data only (Zooplankton only)
}}

\item{Format}{Output format for Pigments data:
\itemize{
\item \code{"all"} - All individual pigment concentrations (default)
\item \code{"binned"} - Pigments grouped into functional classes
}}
}
\value{
A dataframe containing the requested data. Structure varies by
data type (see Details).
}
\description{
A unified interface for retrieving plankton abundance, biovolume, and
biogeochemical data from IMOS marine monitoring programs. This function
consolidates multiple survey-specific functions into a single entry point.
}
\details{
\subsection{Available Data by Survey}{\tabular{ll}{
   Survey \tab Available Types \cr
   NRS \tab Phytoplankton, Zooplankton, Chemistry, Pigments, Pico, Micro, TSS, CTD \cr
   CPR \tab Phytoplankton, Zooplankton \cr
   SOTS \tab Phytoplankton, Zooplankton \cr
   Coastal \tab Micro, Chemistry \cr
   GO-SHIP \tab Micro \cr
}

}

\subsection{Return Format}{

The structure of the returned dataframe varies by data type:

\strong{Wide format} (taxa/variables as columns):
\itemize{
\item Phytoplankton, Zooplankton: Taxa names as columns, samples as rows
\item CTD: Measurement variables as columns, depth bins as rows
}

\strong{Long format} (Parameters and Values columns):
\itemize{
\item Chemistry, Pigments, Pico, Micro, TSS: Standardised long format with
\code{Parameters} column containing variable names and \code{Values} column
containing measurements
}
}

\subsection{Parameter Requirements}{
\itemize{
\item \code{Variable} and \code{Subset} are required for Phytoplankton and Zooplankton
\item \code{Format} is only used for Pigments (defaults to "all")
\item Unused parameters will generate a warning
}
}
}
\examples{
# Get NRS phytoplankton abundance data at genus level
dat <- pr_get_data(Survey = "NRS", Type = "Phytoplankton",
                   Variable = "abundance", Subset = "genus")

# Get CPR zooplankton data
dat <- pr_get_data(Survey = "CPR", Type = "Zooplankton",
                   Variable = "abundance", Subset = "htg")

# Get NRS water chemistry
dat <- pr_get_data(Survey = "NRS", Type = "Chemistry")

# Get NRS pigments in binned format
dat <- pr_get_data(Survey = "NRS", Type = "Pigments", Format = "binned")

# Get NRS CTD profiles
dat <- pr_get_data(Survey = "NRS", Type = "CTD")

# Get GO-SHIP microbial data
dat <- pr_get_data(Survey = "GO-SHIP", Type = "Micro")

# Get Coastal Seas chemistry data
dat <- pr_get_data(Survey = "Coastal", Type = "Chemistry")

}
\seealso{
\code{\link[=pr_get_trips]{pr_get_trips()}} for sampling trip metadata,
\code{\link[=pr_get_Indices]{pr_get_Indices()}} for derived ecological indices,
\code{\link[=pr_get_info]{pr_get_info()}} for station and taxonomic information
}
