% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils_trips.R
\name{pr_get_trips}
\alias{pr_get_trips}
\title{Get sampling trip metadata}
\usage{
pr_get_trips(Survey = "NRS", ...)
}
\arguments{
\item{Survey}{Survey type to retrieve trips for:
\itemize{
\item \code{"NRS"} - National Reference Station trips (default)
\item \code{"CPR"} - Continuous Plankton Recorder trips
}}

\item{...}{Additional arguments passed to \code{\link[=pr_add_Bioregions]{pr_add_Bioregions()}} when
\code{Survey = "CPR"}. Currently supports:
\itemize{
\item \code{near_dist_km} - Distance in kilometres to pad bioregion boundaries when
assigning samples to regions (default behaviour uses exact boundaries)
}}
}
\value{
A dataframe with trip information. Common columns include:
\itemize{
\item \code{TripCode} - Unique trip identifier
\item \code{SampleTime_Local} - Local sampling date and time
\item \code{Year_Local}, \code{Month_Local} - Temporal components
\item \code{Latitude}, \code{Longitude} - Geographic coordinates
}

Additional columns vary by survey:
\itemize{
\item \strong{NRS:} \code{StationCode}, \code{StationName}
\item \strong{CPR:} \code{BioRegion} - Australian marine bioregion assignment
}
}
\description{
Load metadata for sampling trips from the National Reference Stations (NRS)
or Continuous Plankton Recorder (CPR) survey. This unified function replaces
the survey-specific functions \code{pr_get_NRSTrips()} and \code{pr_get_CPRTrips()}.
}
\details{
\strong{NRS Trips:} Returns information about each NRS sampling trip (voyage),
including the trip code, station, date/time, and basic metadata. Only NRS
project trips are included (SOTS trips are excluded). Samples designated as
'P' (plankton) samples or with no sample type designation are included.

\strong{CPR Trips:} The CPR samples continuously as it is towed behind ships of
opportunity. Each "trip" represents a segment of the tow, typically covering
approximately 3 nautical miles. Samples are automatically assigned to
Australian marine bioregions using \code{\link[=pr_add_Bioregions]{pr_add_Bioregions()}}. Bioregions include:
Temperate East, South-east, South-west, North, and North-west.
}
\examples{
# Get NRS trip metadata
dat <- pr_get_trips(Survey = "NRS")

# Get CPR trip metadata with default bioregion assignment
dat <- pr_get_trips(Survey = "CPR")

# Get CPR trips with expanded bioregion boundaries (250 km padding)
dat <- pr_get_trips(Survey = "CPR", near_dist_km = 250)

# Examine sampling frequency by station (NRS)
dat <- pr_get_trips(Survey = "NRS")
table(dat$StationName, dat$Year_Local)

# Examine sampling effort by bioregion and year (CPR)
dat <- pr_get_trips(Survey = "CPR")
table(dat$BioRegion, dat$Year_Local)

}
\seealso{
\code{\link[=pr_get_info]{pr_get_info()}} for station-level metadata,
\code{\link[=pr_add_Bioregions]{pr_add_Bioregions()}} for bioregion assignment details
}
